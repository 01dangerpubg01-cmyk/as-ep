name: Astro EPG Daily Grab

on:
  schedule:
    - cron: "35 16 * * *"

  workflow_dispatch:
    inputs:
      debug:
        description: "Run in debug mode (prints raw API response)"
        required: false
        default: "false"
      keep_days:
        description: "Days of past EPG to keep (default: 14)"
        required: false
        default: "14"
      workers:
        description: "Parallel workers (default: 8)"
        required: false
        default: "8"

jobs:
  grab-epg:
    name: Grab Astro EPG → XMLTV
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Debug API response (only when debug=true)
        if: ${{ github.event.inputs.debug == 'true' }}
        run: python debug_api.py

      - name: Grab & merge EPG data
        run: |
          python grab_epg.py \
            --output astro_epg.xml \
            --keep-days ${{ github.event.inputs.keep_days || '14' }} \
            --workers ${{ github.event.inputs.workers || '8' }} \
            --no-details

      - name: Commit EPG XML
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add astro_epg.xml
          git diff --staged --quiet || git commit -m "chore: update EPG $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push

      - name: Upload as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: astro-epg-xml
          path: astro_epg.xml
          retention-days: 7
